const strStr = (haystack, needle) => {
    return haystack.indexOf(needle)
}

console.log(strStr("abc", "c"))

// Учитывая две строки needle и haystack, верните индекс первого вхождения needlein haystackили, -1если needle
// он не является частью haystack.